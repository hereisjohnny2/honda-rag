# Scripts de shell precisam de fim de linha LF (o Git no Windows converteria para CRLF e quebraria no Linux)
*.sh text eol=lf
Dockerfile text eol=lf
docker-compose*.yml text eol=lf
deploy/Caddyfile text eol=lf
